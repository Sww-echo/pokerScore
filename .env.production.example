# 当前示例用于把应用挂到 https://agentsww.com/poker
# 如果要部署到根路径，把 VITE_APP_BASE_PATH 改成 /，并同步调整 API / Socket 路径
# 正式上线建议使用 HTTPS；如果只是临时联调，可把域名改成 http://agentsww.com
VITE_APP_BASE_PATH=/poker/
VITE_API_BASE_URL=/poker/api/v1
VITE_WS_BASE_URL=https://agentsww.com
VITE_WS_PATH=/poker/socket.io
